platform :ios, '13.0'

target 'IPAAPIServer' do
  # Pods for IPAAPIServer
  
  # WebKit is built-in, no need to add
  
  # Optional: Add other dependencies as needed
  # pod 'Alamofire'
  # pod 'SwiftyJSON'

end
